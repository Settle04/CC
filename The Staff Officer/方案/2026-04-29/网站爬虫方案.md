# 网站爬虫方案

## 需求
- 爬取目标：https://www.86hk.vip/admin/goods
- 数据内容：在售商品信息（列表 + 详情）
- 技术栈：Playwright

## 当前状态
- ~~商品列表爬虫~~：**失效，需重写**
- 商品详情爬虫：已验证可用（285条）

## 最终目标
- **商品列表：** 292 条（30页）
- **商品详情：** 285 条
- **输出文件：**
  - `products-onsale.json` - 商品列表
  - `product-details.json` - 商品详情

## 执行步骤

### Step 1: 验证登录状态
检查 `cookies.json` 是否有效，如失效需重新登录

### Step 2: 重新实现商品列表爬虫
```javascript
// scrape-onsale-products.js
// 遍历30页，抓取292条商品列表数据
// 关键：表格在 Frame 2（addtabs=1）
// 分页选择器：.page-next a，页码从 .page-number.active 获取
```

### Step 3: 验证商品详情爬虫
基于已有代码确认仍可正常抓取"查看资料"

## 关键发现

1. **iframe 嵌套**：表格在 Frame 2（addtabs=1），详情弹窗在 Frame 3（get_document）
2. **分页选择器**：`.page-next a`，页码从 `.page-number.active` 获取
3. **弹窗关闭**：需用 `layer.closeAll()` + 移除 `.layui-layer` 元素
4. **断点续传**：每5条自动保存，支持从已有数据继续

## 文件清单

| 文件 | 说明 |
|-----|-----|
| `login.js` | 登录并保存 cookies |
| `cookies.json` | 登录凭证 |
| `scrape-onsale-products.js` | ~~爬取商品列表~~（需重写） |
| `products-onsale.json` | 商品列表数据 |
| `scrape-all-details.js` | 爬取商品详情（285条） |
| `product-details.json` | 商品详情数据 |